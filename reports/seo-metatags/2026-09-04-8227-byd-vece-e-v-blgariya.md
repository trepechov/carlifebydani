# SEO Optimization — BYD вече е в България: глобалният електрически гигант стъпва официално на нашия пазар

**URL:** https://www.carlifebydani.com/publications/byd-veche-e-v-blgariya-globalniyat-elektricheski-gigant-stpva-oficzialno-na-nashiya-pazar/ · **Post ID:** 8227 · **Category:** publications
**Prepared:** 2026-09-04 (first phase to touch this post)
**Status:** applied
**Keyphrase:** BYD в България
**Ledger:** 8227-2026-09-04-C

---

## Phase A — Keyphrase research
_Written by `seo-keyphrase-research`. Sets `Status: researched` and the `Keyphrase:` line above._

### What this article is about
Publications-category launch article (2026-03-26, modified 2026-06-22)
covering BYD's official entry into the Bulgarian market. Real, substantial
owned prose across 7 H2 sections: the launch itself (led by Maggie Zhou,
distribution via Eurohold and Дару Кар plus other dealers, 6 launch models —
4 EV + 2 hybrid, 3 more by year-end), the model range (Ocean-series EVs:
Seal, Sealion, Atto 3, Dolphin/Dolphin Surf), BYD's global scale (36+
countries, 1000+ showrooms), the upcoming Hungary factory (Q2 production
start, Dolphin + Atto 3 first), and the technology (e-Platform 3.0, Blade
Battery, 230kW/380A fast charging). Heavily illustrated (9 galleries/images,
all with empty `alt`). Zero internal links to other site content anywhere in
the body.

### Current state
| | Value | Length |
|---|---|---|
| `<title>` | *(Yoast title empty → falls back to raw post title)* `BYD вече е в България: глобалният електрически гигант стъпва официално на нашия пазар - Car Life by Dani` | 89 chars pre-suffix, well over budget |
| `<meta name=description>` | *(absent — confirmed no `<meta name="description">` rendered)* | 0 |
| Focus keyphrase | *(empty)* | — |
| H1 | Same as title — long, no distinct SEO-optimized H1/title split | — |
| Owned word count (Yoast `wordCount`) | 670 | — |
| Images without alt | All 9+ inline images/gallery figures — every one checked has `alt=""` (including the featured image, id 8257) | — |
| Internal links out / in | **0 out** — despite directly relevant existing content (see cannibalization/backlink section below). In: not separately pulled — see GSC below, page already gets real organic impressions independent of internal links. | — |
| Heading outline | H1 → 7 clean H2s, no gaps | — |

### Demand research
**GSC (90d, 2026-06-06→2026-09-03, this URL):**
| Query | Impr | Clicks | CTR | Pos |
|---|---|---|---|---|
| byd българия | 15 | 0 | 0.00% | 40.8 |
| byd какво означава | 9 | 0 | 0.00% | 10.3 |
| byd bulgaria | 6 | 0 | 0.00% | 38.0 |
| byd българия контакти | 2 | 0 | 0.00% | 10.0 |
| byd в българия | 1 | 0 | 0.00% | **9.0** |
| byd българия представителство | 1 | 0 | 0.00% | 49.0 |
| byd значение | 1 | 0 | 0.00% | 9.0 |
| maggie zhou byd | 1 | 0 | 0.00% | 13.0 |
| *(page total, incl. anonymized long tail)* | **227** | **0** | **0.00%** | **10.6** |

Zero clicks on 227 impressions/90d — again fully consistent with a completely
absent meta description. But **this one isn't a pure presentation fix**: the
single highest-volume named query, the bare brand+country phrase "byd
българия", sits at position **40.8** — a genuine ranking gap, not a snippet
problem, on the query that matters most by search volume (see below). The
page's much better position on other, more analytical phrasings (`byd какво
означава` pos 10.3, `byd в българия` pos 9.0) shows Google *does* understand
this page is relevant to BYD-in-Bulgaria — just not enough to compete on the
bare navigational phrase against official/dealer sites (see SERP check).

**Google autocomplete (hl=bg, gl=bg):**
| Seed | Completions |
|---|---|
| byd българия | контакти, шоурум, цени, варна, пловдив, сервиз, ценова листа, адрес, **представителство** — dealer/commercial intent dominates |
| byd в българия | **byd вече в българия**, **byd идва в българия**, **byd влиза в българия**, кога, цена, представителство, коли, гаранция, сервиз — a distinct, more informational/announcement-flavored cluster |
| byd на българския пазар | *(no completions)* |

The `в българия` seed's completions (`вече в`, `идва в`, `влиза в`) match this
article's own framing almost exactly — real evidence the informational
"entry announcement" angle has its own demand cluster, separate from the
dealer/shopping cluster the bare phrase pulls.

**Keyword metrics (bg, DataForSEO `search_volume/live`, fresh 2026-09-04):**
| Phrase | Volume/mo | Competition | Trend |
|---|---|---|---|
| `byd българия` | **5,400** | MEDIUM (46) | Huge and recent: 1,600 (Aug 2025) → **12,100 (Mar 2026, launch month)** → settling at 4,400–6,600/mo since. This is the single biggest keyword touched in this project's history. |
| `byd в българия` | no reportable volume | — | below DataForSEO's floor, but see autocomplete above — real informational demand exists, just not enough raw volume to register |
| `byd seal` | 1,600/mo | LOW (25) | model-specific, shopping intent |
| `byd atto 3` | 720/mo | LOW (21) | model-specific, shopping intent |
| `byd dolphin` | 590/mo | LOW (18) | model-specific, shopping intent |
| `byd sealion` | 170/mo | LOW (19) | model-specific, shopping intent |

All banked to `data/seo-cache/keywords.csv`. The model-specific terms carry
real volume too, but this article only briefly describes each model
(1–2 sentences) — not deep enough to be *the* answer for e.g. "byd seal"
shopping intent; noting as a content-gap risk, not proposing to chase them
as the focus.

**SERP check (DataForSEO, "byd българия", bg):** top 7 is **100%
navigational/commercial**: `byd.com/bg` (#1, official), `mobile.bg`
classifieds (#2), `byd.darucar.bg` — one of this article's own named dealers
(#3), a Facebook fan group (#4), `autoline.bg` classifieds (#5),
`escoautomotive.bg` — the other named dealer (#6), and one buyer's-guide blog
post, `goelectric.bg` "BYD в България - модели, цени и впечатления"
(#7, published 2026-04-11 — *after* this article). `related_searches`:
контакти, шоурум, ценова листа, работно време, варна, пловдив, сервиз — all
dealer-directory intent this editorial article was never built to serve.

**Read together:** position 40.8 for the flagship phrase is not a snippet bug
— it's this article competing against BYD's own official site and both
dealers it names, for a query that's mostly people looking for a showroom or
a price list. That fight isn't winnable with metatags. But the page already
earns 227 impressions/90d and ranks respectably (pos 9–13) for the more
analytical "what does this mean" / "BYD is now in Bulgaria" phrasing — that's
the real, winnable target, and it's exactly what this article's own title and
content deliver that goelectric.bg's price-listicle doesn't.

**GA4:** attempted, hit a filter-syntax error on `landingPage`, not worth a
second round-trip — the GSC+SERP evidence already gives a clear, specific
diagnosis (mixed presentation problem on the analytical phrasing, real
content-fit gap on the bare commercial phrase) without it.

### Recommendation
**Focus keyphrase:** `BYD в България` — matches the article's own title and
actual content (an entry-announcement analysis, not a price/dealer
listicle), already ranks position 9.0 for the exact phrase (n=1, but
consistent with the 10.3/10.0/9.0 cluster of related analytical queries),
and is validated by real autocomplete demand (`byd вече/идва/влиза в
българия`). **Not** targeting the much bigger `byd българия` (5,400/mo) —
that phrase's SERP is dominated by BYD's own official site and this
article's own named dealers; chasing it would be competing with sources this
site cannot out-rank and shouldn't try to.

**Secondary:** `byd какво означава` (matches an actual ranking query),
`byd вече в България` (autocomplete-validated informational phrasing),
`Maggie Zhou BYD` (the one named-person query already showing impressions)

### Proposed metatags
| Field | Before | After | Chars |
|---|---|---|---|
| `_yoast_wpseo_title` | *(empty → falls back to 89-char raw title, way over budget)* | `BYD вече е в България — какво означава` | 39 (+19 suffix = 58) |
| `_yoast_wpseo_metadesc` | *(empty → none rendered)* | `BYD официално влезе в България с 6 модела през Eurohold и Дару Кар. Разказваме какво означава навлизането на глобалния електрически гигант у нас.` | 148 |
| `_yoast_wpseo_focuskw` | *(empty)* | `BYD в България` | — |

### Proposed tags
| Tier | Tag | id | Existing count | Verdict |
|---|---|---|---|---|
| Entity | BYD | 83 | 5 | keep — in-band, the article's whole subject |
| Entity | България | 398 | 5 | keep — in-band, matches the market-entry angle |
| ? | Ocena X | 399 | 1 | **flagging for Phase C to reconsider** — below the 3–10 band (used only on this post), and doesn't clearly match anything in the article: no model called "Ocean X" appears (the article names Seal, Sealion, Atto 3, Dolphin/Dolphin Surf under the "Ocean series" platform name) — looks like a leftover/mistyped tag from post creation, not a deliberate choice |

**Gaps — not created:** the article gives real, named coverage to specific
models (Seal, Sealion, Atto 3, Dolphin) and technology (Blade Battery,
e-Platform 3.0) with real search volume behind several of them (seal 1,600,
atto 3 720, dolphin 590), but **no existing tags** for any of them
(`/wp/v2/tags?search=Seal|Atto|Dolphin` all return zero). Skipping per the
reuse-only rule. Worth a deliberate batch-creation decision if BYD
model-specific coverage becomes a recurring pattern (this site doesn't yet
have per-model BYD content the way it does for Zeekr/Tesla).

### Internal links — research for Phase C
**Outbound (this article should link out):**
1. **Китайските електромобили са тук, за да останат** (id 4115, 2024-04-14,
   `/publications/kitajskite-elektromobili-sa-tuk-za-da-ostanat/`) — **this is
   the flagged same-story-sequel citation already logged in
   `docs/SEO_BACKLINK_TARGETS_TODO.md`** from the 4115 optimization pass
   (2026-09-04): 4115 anticipated Chinese EV makers' European ambitions in
   2024 (BYD scale, tariffs, Japanese-invasion parallel); this article (8227,
   2026-03-26) is the real, confirmed follow-through. New → old direction is
   correct here (8227 is newer), so the edit belongs on **this** post, not on
   4115 — exactly as that flag specified. Natural anchor: the opening
   paragraph's "BYD официално навлиза в България" / global-scale framing, or
   the "Част от глобална експанзия" H2 section discussing BYD's 36-country
   presence — either reads naturally as "the trend we already covered in
   depth."

**Inbound (older posts that should link here):** checked two real candidates
predating 2026-03-26 with strong topical fit — **#EV103 – BYD навлиза в
Румъния с бързото разширяване на търговската си мрежа** (id 6898,
2025-05-10) and **#EV105 – Най-продавания електромобил на BYD пристигна в
Европа** (id 6918, 2025-05-27), both EV News posts narrating BYD's earlier
regional (Romania/Europe) expansion — genuinely the same continuing
expansion story, correctly flowing forward in time to "BYD now in Bulgaria."
**But both have zero body paragraphs** — checked 6898's raw `post_content`:
a bare YouTube embed, no text at all (same EV-News-thin-content pattern
documented elsewhere in this project). No natural sentence to extend with a
link without inventing one from scratch, which is out of scope here (that's
a Phase-B-shaped job, and Phase B doesn't touch arbitrary sibling posts).
Proposing zero inbound links this pass rather than forcing either.

Cannibalization check: `/wp/v2/search?search=BYD в България` and `?search=BYD
българия` → no other post targets this phrase as its own focus keyphrase.
`#EV144 – BYD вече е в България` (id 8319, category ev-news, 2026-03-31) is
the companion EV News episode for the same launch event but is **newer** than
this post by 5 days — not usable as an inbound source, and this post has no
obligation to link forward to it. Clear to proceed with this post's own
keyphrase.

---

## Phase C — Apply
_Written by `seo-article-apply`._

### On-page changes proposed
- [ ] **H1** — no change; already carries the entities naturally.
- [ ] **Subheadings** — no change; 7 clean H2s already cover the content well.
- [ ] **Featured image alt + title** (media id 8257) — this image is embedded
  via its own `wp:image` block directly in `post_content` (not purely as
  `featured_media`), so per the known trap documented on post 4115, fixing
  it needs **two separate writes**: the media library record AND the static
  `alt=""` baked into the block itself.
  - Media library `alt_text`: *(empty)* → `BYD вече е в България — официален старт на пазара`
  - Media library `title`: *(empty)* → `BYD в България`
  - `post_content`'s own block `alt` attribute: `alt=""` → `alt="BYD вече е в България — официален старт на пазара"`
- [ ] **Other 8 inline/gallery images** — also all `alt=""`, same static-block
  pattern. **Not proposing to fix all of them this pass** — scope note: doing
  it correctly needs one block-level edit per image (8 separate `wp:image`/
  `wp:gallery` edits in one already-large post), which is a materially bigger
  content edit than the rest of this pass. Flagging as a worthwhile follow-up,
  not bundling it into this approval.

### Internal links
**Outbound — this article should link to:**
| Target post | URL | Anchor text | Where |
|---|---|---|---|
| Китайските електромобили са тук, за да останат (id 4115, 2024-04-14) | `/publications/kitajskite-elektromobili-sa-tuk-za-da-ostanat/` | „анализирахме подробно" | End of the "Част от глобална експанзия" paragraph |

This is the flagged same-story-sequel citation from `docs/SEO_BACKLINK_TARGETS_TODO.md`
(logged during the 4115 optimization pass) — new (8227, 2026-03-26) → old
(4115, 2024-04-14), the correct direction. **This edit is to this post's own
`post_content`** (not a foreign-post inbound-link edit), verified via exact
string match + full diff before writing (see mechanics below) — confirmed
the only two changes are this link insertion and the featured-image alt fix,
byte-identical everywhere else.

**Inbound — none proposed.** See Phase A: the two date-valid candidates
(#EV103 id 6898, #EV105 id 6918 — both 2025-05, both narrating BYD's earlier
regional expansion) have zero body paragraphs (bare video embeds), no
natural sentence to extend without inventing one from scratch.

**Mechanics used for the two `post_content` edits:** fetched `content.raw`
fresh via authenticated `curl` (not retyped — a first manual-transcription
attempt was caught by a structural sanity check to have silently dropped an
entire paragraph + gallery block, confirming the 4115-documented risk of
retyping large blocks by hand), applied both changes via exact-match Python
string replacement (each old string confirmed to occur exactly once before
replacing), and diffed old vs. new: only the two intended hunks differ,
everything else byte-identical.

### Applied
- [x] Metatags — title / metadesc / focuskw written, confirmed live 2026-09-04 (`<title>` and `<meta name="description">` both render the new values)
- [x] Tags — `[83, 398]` (BYD, България); dropped `Ocena X` (id 399) per approval
- [x] `post_content` written — outbound link to 4115 + featured-image block alt fix. Written via authenticated `curl` with a programmatically-constructed JSON payload (not through the chat-facing tool call, to avoid retyping a 16.7KB string) — post-write fetch confirmed byte-identical to the intended content
- [x] Media library alt + title written — media id: 8257, confirmed in write response
- [x] Auto-linked `/tag/` count inside body prose (post-write): `tag/byd/` 7×, `tag/blgariya/` 7× (mobile+desktop tag-pill duplication, same known pattern as other posts this session — not a body-prose auto-link count specifically, that would need isolating just the article body's own `the_content` output)

**Transcription-risk note, worth keeping in this project's institutional memory:**
a first manual attempt to retype this post's ~16.5KB `content.raw` (to build
the diff locally) silently dropped an entire paragraph + gallery block —
caught only because a structural sanity check (block/paragraph counts,
distinctive-phrase grep) was run before writing, not by the diff itself
(which was diffing my own possibly-wrong copy against itself). Recovered by
fetching `content.raw` fresh via **authenticated `curl`** (macOS Keychain
entry `carlifebydani-wp-mcp`, user `seo-bot`) straight to a file, applying
both edits via exact-match Python string replacement (each old string
asserted to occur exactly once), diffing old vs. new pre-write, and writing
back via `curl` from that same file — no manual retyping anywhere in the
final path. This is a stronger version of the 4115 lesson ("fetch fresh via
curl immediately before constructing the diff") — worth doing this way by
default for any post_content edit past a trivial one-sentence insert,
especially on image-heavy `publications`-category posts like this one.

### Declined
| Group | What was proposed | Reason declined | Date |
|---|---|---|---|

### Risks / notes
This is a mixed case, not a pure presentation fix: the page already earns
227 impressions/90d at position 10.6 for a cluster of analytical BYD+Bulgaria
queries (real win available from metatags), but the single highest-volume
named query (`byd българия`, 5,400/mo) sits at position 40.8 against a
100%-navigational/commercial SERP (BYD's own official site + both dealers
this article names) that this editorial piece was never going to out-rank.
The chosen keyphrase deliberately targets the winnable analytical angle, not
the much bigger but unwinnable commercial phrase — said explicitly so the
measurement plan doesn't get judged against the wrong number.

### Measurement
Baseline (GSC, 2026-06-06→2026-09-03, 90d): impressions 227, clicks 0, CTR
0.00%, position 10.6. Re-check after 2–4 weeks (metatags/tags/alt/outbound
link — no new prose paragraph, so no extended re-crawl wait needed). Ledger
row: *(set on apply)*, `verify_due`: *(date_applied + 28d)*.
