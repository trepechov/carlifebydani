# SEO Optimization — VW ID.4 GTX – Впечатляващо комфортен, стилен и практичен

**URL:** https://www.carlifebydani.com/ev-review/vw-id4-gtx-vpechatlyavashto-komforten-stilen-praktichen/ · **Post ID:** 130 · **Category:** ev-review
**Prepared:** 2026-09-05 (first phase to touch this post)
**Status:** applied
**Keyphrase:** VW ID.4 GTX
**Ledger:** `130-2026-09-05-C`

**How this post surfaced:** direct user request (post id 130), independent
of any existing cluster/backlog.

---

## Phase A — Keyphrase research
_Written by `seo-keyphrase-research`. Sets `Status: researched` and the `Keyphrase:` line above._

### What this article is about
A **477-word** technical review of the **Volkswagen ID.4 GTX**, structured
with real H3 subheadings (Технически Данни, Разлики между ID.4 и ID.5,
Значението на MEB Платформата, Сравнение с Tesla Model Y, Цена, Заключение).
By a different author than most of this site's reviews ("Мишо", not "Дани").
Notably **generic/spec-sheet in tone** — no first-person driving impressions,
no concrete anecdotes, and the "Цена" section never states an actual price
figure (only "едно от най-достъпните SUV... по отношение на цена-качество").
Real facts present: ~77kWh battery, up to 520km WLTP range, 150–220kW
(204–299hp) depending on trim, ~30min fast-DC top-up, MEB platform (shared
across the VW Group), ID.4 vs ID.5 body-shape/aero differences, and a size
comparison against Tesla Model Y. One existing internal link ("MEB
платформата на VW" → post 1404) — a genuine further-reading reference to an
evergreen platform explainer, not a same-story-sequel, fine regardless of
date even though 1404 (2024-02-04) postdates this post.

### Current state
| | Value | Length |
|---|---|---|
| `<title>` | *(empty → falls back to post title)* `VW ID.4 GTX - Впечатляващо комфортен, стилен и практичен - Car Life by Dani` | ~78 chars, over budget |
| `<meta name=description>` | absent | — |
| Focus keyphrase | none set | — |
| H1 | matches post title verbatim | — |
| Owned word count | **477** (Yoast schema) | — |
| Images without alt | 6 gallery images (all `alt=""`) + featured image media 1129 (`alt=""`, `title` a raw slug) | |
| Internal links out / in | **1 outbound** (to post 1404, but via a broken 2-hop redirect chain — see below) / 0 inbound confirmed | |

**Broken-redirect finding:** the existing link to post 1404 uses
`http://carlifebydani.com/publications/meb-и-meb-платформата-на-volkswagen-group/`
(no `https`, non-ASCII Cyrillic slug) — resolves, but only via **two chained
301 redirects** (http→https, then old slug→canonical ASCII slug
`meb-i-meb-platformata-na-volkswagen-group`) before reaching the live page.
A real, if minor, technical SEO cost (extra latency, diluted link equity
across hops) — proposed to fix by pointing the href straight at the
canonical URL, same anchor text, in Phase C below.

`post_excerpt` is non-empty — noted per the standing trap, not touched.

### Demand research
**GSC (90d, 2026-06-06→2026-09-03, page-level, this URL — reused from the
shared cached pull):** **279 impressions, 3 clicks**, across 76 queries —
substantial existing demand. Top rows:
| Query | Impr | Clicks | Pos |
|---|---|---|---|
| volkswagen id 4 цена | 28 | 0 | 9.3 |
| vw gtx | 23 | 0 | 10.0 |
| vw id 4 gtx | 21 | 0 | **7.0** |
| volkswagen gtx цена | 17 | 0 | 9.7 |
| vw id 4 | 13 | 1 | 12.4 |
| volkswagen gtx | 10 | 0 | 9.2 |

Position 7–10 across a real query set — a "distance beats volume"
presentation case. **"цена" (price) is the dominant recurring modifier**
across nearly every top query — a real signal that the article's own vague
price-positioning section (no actual number) is likely costing clicks, not
something metatags alone can fix (can't invent a number that isn't in the
article).

**Cannibalization check:** `/wp/v2/search?search=ID.4` surfaces post
**1060** ("Пътуваме с VW ID.4 GTX и Hyundai IONIQ 6 AWD... EVTour400",
2023-12-23, postdates this post, not yet optimized) — also carries "VW
ID.4 GTX" directly in its title. Different angle though: 1060 is a specific
range/consumption comparison road-trip test, this post (130) is the general
spec/technical review. **Flagging for 1060's own future Phase A**: it
should target a consumption/comparison-specific phrase (e.g. "VW ID.4 GTX
разход" or an IONIQ 6 comparison angle) when optimized, not the bare "VW
ID.4 GTX" — that's now owned here as the general review.

**Keyword metrics (bg, DataForSEO, fresh 2026-09-05):**
| Phrase | Volume/mo | Note |
|---|---|---|
| volkswagen id.4 | 1,000 | far the largest — but too generic for this GTX-specific article, would be a keyphrase/content mismatch |
| vw gtx | 140 | real, but ambiguous (could mean any VW GTX trim, not just ID.4) |
| volkswagen id4 цена | 140 | real, price-intent — but article has no number to satisfy it |
| vw id4 gtx | **50** | matches this article's actual GTX-specific subject exactly, and GSC's own best-positioned real query — the pick |
| id4 gtx | 20 | thin |

All banked to `data/seo-cache/keywords.csv`.

### Recommendation
**Focus keyphrase:** `VW ID.4 GTX` — matches the article's actual GTX-
specific subject (not the much broader base ID.4), real volume (50/mo),
and directly corresponds to GSC's own best-positioned query
(`vw id 4 gtx`, 21 impr, pos 7.0).
**Secondary:** `MEB платформа`, `ID.4 срещу Tesla Model Y`, `VW ID.4 GTX
цена` (used carefully in the metadesc — positioning language only, no
invented number).

### Proposed metatags
| Field | Before | After | Chars |
|---|---|---|---|
| `_yoast_wpseo_title` | *(empty → falls back to post title, ~78 chars, over budget)* | `VW ID.4 GTX: пълни спецификации и цена %%sep%% %%sitename%%` | 38 + 19 suffix = 57 |
| `_yoast_wpseo_metadesc` | *(absent)* | `VW ID.4 GTX: до 520 км пробег, 220 kW мощност и бързо зареждане за 30 мин. Сравнихме го с ID.5 и Tesla Model Y. Разгледахме и MEB платформата.` | 142 |
| `_yoast_wpseo_focuskw` | *(empty)* | `VW ID.4 GTX` | — |

Every metadesc fact traces to the real prose (520km WLTP, 220kW/299hp top
power, ~30min DC fast charge, the ID.5 and Tesla Model Y comparison
sections, MEB platform) — no price figure invented despite "цена" being the
dominant GSC search modifier, since the article itself never states one.

### Proposed tags
No changes — existing set already correct:
| Tier | Tag | id | Existing count | Verdict |
|---|---|---|---|---|
| Entity | GTX | 13 | 2 | ✅ keep despite below-band — the headline trim itself |
| Entity | ID.4 | 12 | 4 | ✅ keep — in-band, named repeatedly |
| Entity | MEB | 55 | 9 | ✅ keep — in-band, a full section devoted to it |
| Entity | VW | 9 | 13 | ✅ keep — established (11–19 band), the brand itself |

**Considered and declined:** `Tesla` (id 4, count 59 — a saturated brand
hub) for the Model Y comparison section. Not adding — Tesla/Model Y is a
comparison subtopic here, not this article's headline entity, and the
ceiling exception only applies when a saturated brand genuinely is the
headline (it isn't). No `Model Y` or `ID.5` tag exists on this site at all
— both are single passing comparison mentions, not creating speculatively.

---

## Phase C — Metatags, tags, alt text, links
_Written by `seo-article-apply`. Advances `Status: applied` for whatever was
approved._

### Internal links — research
**Existing link fix (not a new link):** update the href on the existing
"MEB платформата на VW" anchor from the broken 2-hop-redirect URL to the
canonical `https://www.carlifebydani.com/publications/meb-i-meb-platformata-na-volkswagen-group/`
— same anchor text, same target post (1404), just removing the redirect
chain.

**Inbound:** none proposed. Light-checked posts predating 130 (2023-10-08)
for a natural VW/ID.4-adjacent anchor — post 127 ("Светлин Стайков — VW
e-Golf, VW ID.3", 2023-09-02, an EV Masters owner-story) is topically
closest but covers different models (e-Golf/ID.3, not ID.4 GTX); no
verbatim anchor to hang a link on without inventing text. Not pursuing
further for this single-post pass.

**Outbound:** none new. Post 1060 (the ID.4 GTX/IONIQ 6 consumption test,
2023-12-23) is topically close but nothing in 130's own text discusses a
specific consumption test to link from — flagging as a candidate for a
future pass once 1060 gets its own optimization, rather than forcing an
ungrounded sentence now.

### Proposed image alt + title (media 1129, featured image only — the 6
gallery images are a small, separate set worth a quick follow-up but out
of scope for this pass, same discipline as larger galleries elsewhere)
| Field | Before | After |
|---|---|---|
| `alt_text` | *(empty)* | `Volkswagen ID.4 GTX — електрически SUV на VW` |
| `title` | `vw-id4-gtx-vpechatlyavashto-komforten-stilen-praktichen` (raw slug) | `VW ID.4 GTX` |

### Applied
- [x] Metatags — title / metadesc / focuskw written, confirmed live
  (`<title>`, `<meta name="description">` and `og:description` all match
  exactly).
- [x] Tags — unchanged, confirmed still `[9, 12, 13, 55]`.
- [x] Image alt + title — media 1129 written and confirmed live.
- [x] Existing internal link fixed — the "MEB платформата на VW" anchor now
  points directly at `https://www.carlifebydani.com/publications/meb-i-meb-platformata-na-volkswagen-group/`
  (no redirect hops), same anchor text. Built and verified the spliced
  content programmatically (single-occurrence fragment match, all block
  counts unchanged, link count 1→1, char delta exactly the 5-char URL
  difference) before sending via `curl` with the `seo-bot` Keychain
  password. Confirmed byte-identical live response.
- [x] Auto-linked `/tag/` count inside body prose: **9** (`ID.4` ×5, `MEB`
  ×4) — `ID.4` linking 5 times confirms yet again the theme's 1×-per-tag
  cap fix is not deployed live (same finding as every other post this
  session). `GTX` and `VW` did not auto-link despite being tags on this
  post — not investigated further, noted for completeness.

### Declined
_None — all three approval-gate groups (metatags, image alt/title, link
fix) were approved in full._

### Risks / notes
- The article's vague "Цена" section (no real number) likely caps CTR on
  the dominant "цена" search modifier — a genuine content gap, but fixing
  it means adding a real price to the body copy (Phase B/content-edit
  territory), not something this phase can address without inventing a
  figure.
- 6 gallery images still lack alt text — small enough to be a quick
  follow-up, unlike the 24–62-image galleries elsewhere in this pipeline.
- Flagged for a future pass: post 1060 should avoid "VW ID.4 GTX" as its
  own keyphrase once optimized (now owned here) — pick a consumption/
  comparison-specific angle instead.

### Measurement
Baseline (GSC, 2026-06-06→2026-09-03): 279 impr / 3 clicks / 1.08% CTR
(blended) / ~9 pos (blended across top queries). Re-check in 2–4 weeks —
real existing impressions to build on.
